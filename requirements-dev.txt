# Development / test tooling — runtime deps live in requirements.txt.
# Install both:  pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
ruff>=0.6.0
